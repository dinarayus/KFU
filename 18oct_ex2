#include<iostream>
using namespace std;

void back_mass(int N) {
	int *array;
	array = new int[N];
	for (int i = 0; i < N; i++) {
		cin >> array[i];
	}
	for (int i = N - 1; i >= 0; i--)
	{

		cout << array[i] << " ";

	}
}

int main() {
	int num;
	cin >> num;
	back_mass(num);

}
